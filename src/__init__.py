# utils（utilities 的缩写）是项目的 “工具箱”，存放不绑定具体业务、全项目通用的工具函数 / 类—— 这些代码可以脱离业务逻辑独立存在，任何模块都能调用。
# 1. 核心作用
# 封装通用功能，避免重复代码（比如时间格式化、数据加密、文件操作）；
# 简化业务代码，让 service/api 层只关注核心逻辑；
# 统一工具类的实现，方便维护（比如所有加密逻辑都放 utils/security.py）。